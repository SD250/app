<h1>Panel Administratora - Edytuj biurko</h1>

<%= form_with model: @desk, url: update_desk_path(@desk), method: :patch do |form| %>
  <% if @desk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@desk.errors.count, "error") %> prohibited this desk from being saved:</h2>

      <ul>
        <% @desk.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :location %><br>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :status %><br>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit "Zapisz zmiany" %>
  </div>
<% end %>

<%= link_to 'Powrót', admin_desks_path %>